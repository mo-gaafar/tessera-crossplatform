import 'package:flutter/material.dart';

/// Class containing all the colors used in the app.
class AppColors {
  ///  The primary color of the app.
  static const Color primary = Color(0xffd1410c);

  /// The secondary color of the app.
  static const Color secondary = Color(0xff3659e3);

  /// The background color of the app when viewed in [lightTheme].
  static Color lightBackground = Colors.grey.shade200;

  /// The font color of text on a [lightBackground].
  static const Color textOnLight = Color(0xff1e0a3c);

  /// The secondary font color of text on a [lightBackground].
  static const Color secondaryTextOnLight = Color(0xff39364f);
}
